import { ComingSoon } from "@/components/shared/coming-soon";

export default function LeadsPage() {
  return (
    <ComingSoon
      title="Leads"
      description="Kanban par stage, import CSV, enrichissement Pappers, prospection Google Maps, score IA, actions bulk."
      phase="Phase 2"
    />
  );
}
