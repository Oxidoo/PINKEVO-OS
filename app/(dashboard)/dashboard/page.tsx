import { ComingSoon } from "@/components/shared/coming-soon";

export default function DashboardPage() {
  return (
    <ComingSoon
      title="Tableau de bord"
      description="KPI cards animées (MRR, clients actifs, leads chauds, marge), graphique CA vs coûts, activité temps réel, wins du mois."
      phase="Phase 11"
    />
  );
}
