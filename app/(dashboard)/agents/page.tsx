import { ComingSoon } from "@/components/shared/coming-soon";

export default function AgentsPage() {
  return (
    <ComingSoon
      title="Agents IA"
      description="5 agents (prospector, qualifier, proposal writer, seo auditor, perf auditor) avec runs async + tracking de coûts."
      phase="Phase 4"
    />
  );
}
